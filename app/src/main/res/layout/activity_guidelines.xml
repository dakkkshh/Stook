<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Guidelines"
    android:background="@drawable/guideline_back"
    android:fitsSystemWindows="true"
    android:scaleType="fitXY"
    >

    <TextView
        android:id="@+id/guideline_heading"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:text="@string/guidelines_txt"
        android:textAllCaps="true"
        android:textColor="@color/black"
        android:fontFamily="@font/raleway_bold"
        android:textAlignment="center"
        android:textSize="35sp"
        android:layout_marginTop="10dp"/>
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/guideline_heading"
        android:textColor="@color/black"
        android:fontFamily="@font/raleway_semibold"
        android:textAlignment="center"
        android:text="@string/sample_guideline"
        android:textSize="22sp"/>
</androidx.constraintlayout.widget.ConstraintLayout>